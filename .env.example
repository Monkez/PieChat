# =============================================================================
# PieChat Production Environment
# =============================================================================
# Copy this file to .env and update the values before deploying
# =============================================================================

# ─── Domain Configuration ───────────────────────────────────────────────────
# Your domain name (e.g., chat.example.com)
DOMAIN=chat.example.com

# Matrix server name — used for user IDs (@user:server_name)
# Usually same as DOMAIN, set this ONCE and never change
MATRIX_SERVER_NAME=chat.example.com

# Email for Let's Encrypt SSL certificate
SSL_EMAIL=your-email@example.com

# ─── Security ───────────────────────────────────────────────────────────────
# Dendrite registration shared secret (change this!)
REGISTRATION_SHARED_SECRET=change_me_to_a_random_string_2026

# CORS origins allowed to connect
CORS_ORIGINS=https://chat.example.com

# ─── Auth Service ───────────────────────────────────────────────────────────
# Dev password for testing (remove in real production)
DEV_MATRIX_PASSWORD=Pass@12345

# Admin secret key for auth admin endpoints
ADMIN_SECRET=piechat-admin-change-me-2026

# SMS Provider (uncomment when ready)
# SMS_PROVIDER=webhook
# SMS_BRANDNAME=PieChat
# SMS_WEBHOOK_URL=https://your-sms-gateway.com/api/send

# Redis URL for persistent auth state (optional, falls back to in-memory)
# REDIS_URL=redis://localhost:6379

# ─── OTP & Security Settings (optional, defaults shown) ─────────────────────
# OTP_WINDOW_MS=600000          # 10 min — OTP request rate limit window
# MAX_OTP_REQUESTS=5            # Max OTP requests per window
# OTP_EXPIRY_MS=300000          # 5 min — OTP code validity period
# OTP_CODE_LENGTH=6             # OTP code digit length
# PASSWORD_MAX_FAILURES=5       # Max wrong attempts before lock
# PASSWORD_BLOCK_MS=900000      # 15 min — account lock duration
# OTP_MAX_FAILURES=5            # Max wrong OTP before lock
# OTP_BLOCK_MS=600000           # 10 min — OTP verify lock duration
# TRUSTED_DEVICE_EXPIRY_DAYS=90 # How long a trusted device stays trusted

